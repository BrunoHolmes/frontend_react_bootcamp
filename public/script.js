/**
 * Babel: Converter (transpilar) o código do React para um código que o browser entenda
 *
 * Webpack: Para cada tipo de arquivo (.js, .css, .png, etc) vai converter o código de uma maneira diferente
 *
 * Loaders (usado pelo webpack): babel-loader, css-loader, image-loader, file-loader, etc.
 */